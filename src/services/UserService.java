package services;

import models.User;

public class UserService {
    public User logIn(String email, String password) {
        // ToDo: Fetch user from database and return it
        return new User();
    }
}
